name: Build and deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1

      - name: Clear Cache 🧹
        uses: actions/cache@v3
        with:
          path: |
            node_modules
            .parcel-cache  # Очищення кешу Parcel (або будь-яких інших директорій, які ви використовуєте)
          key: ${{ runner.os }}-node-modules-clear-${{ github.run_id }}  # Унікальний ключ для оновлення кешу

      - name: Install and Build 🔧
        run: |
          npm ci
          npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@4.1.0
        with:
          branch: gh-pages
          folder: dist
